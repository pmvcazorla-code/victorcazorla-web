---
---
const title = "Víctor Cazorla — Medio Ambiente, Docencia y Ética Profesional";
const description = "Ambientólogo colegiado. Proyectos ambientales, docencia especializada y compromiso ético con el territorio y las personas.";
---

<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content={description} />
  <title>{title}</title>

  <style>
    :root {
      --bg: #0b1220;
      --bg-soft: #121a2f;
      --primary: #5fa8ff;
      --accent: #7ce7c9;
      --text: #e6ebf5;
      --muted: #9aa4bf;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI",
        Inter, Helvetica, Arial, sans-serif;
      background: linear-gradient(180deg, var(--bg), var(--bg-soft));
      color: var(--text);
      line-height: 1.6;
    }

    a {
      color: var(--primary);
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }

    header {
      padding: 6rem 1.5rem 4rem;
      max-width: 1100px;
      margin: 0 auto;
    }

    .hero {
      display: grid;
      gap: 2.5rem;
    }

    .eyebrow {
      color: var(--accent);
      font-weight: 600;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      font-size: 0.85rem;
    }

    h1 {
      font-size: clamp(2.5rem, 5vw, 4rem);
      line-height: 1.15;
      margin: 0;
    }

    .lead {
      font-size: 1.25rem;
      color: var(--muted);
      max-width: 720px;
    }

    .cta {
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
      margin-top: 1.5rem;
    }

    .button {
      padding: 0.9rem 1.4rem;
      border-radius: 8px;
      font-weight: 600;
      border: 1px solid transparent;
      transition: all 0.2s ease;
    }

    .button.primary {
      background: var(--primary);
      color: #0b1220;
    }

    .button.primary:hover {
      transform: translateY(-1px);
      filter: brightness(1.05);
    }

    .button.secondary {
      border-color: var(--primary);
      color: var(--primary);
      background: transparent;
    }

    .button.secondary:hover {
      background: rgba(95, 168, 255, 0.1);
    }

    section {
      padding: 5rem 1.5rem;
      max-width: 1100px;
      margin: 0 auto;
    }

    .section-title {
      font-size: 2rem;
      margin-bottom: 1rem;
    }

    .section-intro {
      color: var(--muted);
      max-width: 700px;
      margin-bottom: 3rem;
    }

    .grid {
      display: grid;
      gap: 2rem;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    }

    .card {
      background: rgba(255, 255, 255, 0.03);
      border: 1px solid rgba(255, 255, 255, 0.06);
      border-radius: 14px;
      padding: 2rem;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .card:hover {
      transform: translateY(-4px);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.25);
    }

    .card h3 {
      margin-top: 0;
      margin-bottom: 0.75rem;
    }

    footer {
      border-top: 1px solid rgba(255, 255, 255, 0.08);
      padding: 3rem 1.5rem;
      text-align: center;
      color: var(--muted);
      font-size: 0.95rem;
    }
  </style>
</head>

<body>
  <header>
    <div class="hero">
      <span class="eyebrow">Ambientología · Docencia · Ética</span>

      <h1>
        Pensar el territorio.<br />
        Actuar con responsabilidad.
      </h1>

      <p class="lead">
        Soy <strong>Víctor Cazorla</strong>, ambientólogo colegiado.
        Trabajo en la intersección entre medio ambiente, gestión pública,
        docencia especializada y ética profesional.
      </p>

      <div class="cta">
        <a href="/cienciasambientales" class="button primary">
          Ciencias Ambientales
        </a>
        <a href="/docencia" class="button secondary">
          Docencia
        </a>
        <a href="/deontologia" class="button secondary">
          Deontología profesional
        </a>
      </div>
    </div>
  </header>

  <section>
    <h2 class="section-title">Áreas de trabajo</h2>
    <
